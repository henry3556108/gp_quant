"""
Portfolio-Based GP Evolution Experiment with Niching

這是一個啟用 Niching 策略的實驗配置範例。
只需修改 CONFIG 中的 'niching_enabled' 參數即可啟用/停用 Niching。

使用方式:
    python run_portfolio_experiment_with_niching.py
"""

# 這個腳本是 run_portfolio_experiment.py 的簡化版本
# 主要用於演示如何啟用 Niching 策略

# 只需修改 run_portfolio_experiment.py 中的配置即可：
# CONFIG = {
#     ...
#     'niching_enabled': True,  # 啟用 Niching
#     'niching_n_clusters': 5,
#     'niching_cross_ratio': 0.8,
#     'niching_update_frequency': 5,
#     'niching_algorithm': 'kmeans',
#     ...
# }

print("""
================================================================================
Portfolio GP Evolution with Niching - 使用指南
================================================================================

要啟用 Niching 策略，請修改 run_portfolio_experiment.py 中的配置：

1. 找到 CONFIG 字典（約在第 40 行）

2. 修改 Niching 相關參數：
   
   CONFIG = {
       ...
       # Niching 配置
       'niching_enabled': True,              # 改為 True 啟用
       'niching_n_clusters': 5,              # Niche 數量
       'niching_cross_ratio': 0.8,           # 跨群交配比例 (80%)
       'niching_update_frequency': 5,        # 每 5 代更新相似度矩陣
       'niching_algorithm': 'kmeans',        # 聚類演算法
       ...
   }

3. 運行實驗：
   python run_portfolio_experiment.py

================================================================================
參數說明
================================================================================

niching_enabled (bool):
  - False: 使用傳統 tournament selection（預設）
  - True: 使用跨群選擇策略
  
niching_n_clusters (int):
  - Niche 數量，建議 3-10
  - 太少: 多樣性不足
  - 太多: 每個 niche 太小，選擇壓力不足
  
niching_cross_ratio (float):
  - 跨群交配比例，範圍 [0, 1]
  - 0.0: 完全群內配對
  - 0.8: 80% 跨群，20% 群內（推薦）
  - 1.0: 完全跨群配對
  
niching_update_frequency (int):
  - 每 N 代重新計算相似度矩陣
  - 建議 5-10 代
  - 太頻繁: 計算開銷大
  - 太稀疏: niche 更新不及時
  
niching_algorithm (str):
  - 'kmeans': K-means 聚類（推薦）
  - 'hierarchical': 層次聚類

================================================================================
預期效果
================================================================================

啟用 Niching 後，你會看到：

1. 每 N 代顯示相似度計算和聚類資訊：
   🔬 Niching: 計算相似度矩陣...
   ✓ 相似度矩陣計算完成 (12.3s)
   平均相似度: 0.3245
   多樣性分數: 0.6755
   
   🔬 Niching: 聚類（k=5）...
   ✓ 聚類完成
   Silhouette 分數: 0.2841
   各 Niche 大小: {0: 1023, 1: 987, 2: 1045, 3: 956, 4: 989}

2. 選擇階段顯示跨群統計：
   🎯 使用跨群選擇...
   ✓ 選擇完成
   跨群配對: 2000 (80%)
   群內配對: 500 (20%)

3. 日誌文件包含 Niching 統計：
   evolution_log.json 中會有 'niching' 欄位
   記錄每代的多樣性指標

================================================================================
向下兼容性
================================================================================

✅ 完全向下兼容！

- niching_enabled = False: 使用原有的 tournament selection
- niching_enabled = True: 使用新的跨群選擇

所有現有實驗腳本無需修改即可繼續運行。

================================================================================
小規模測試建議
================================================================================

首次測試建議使用較小的配置：

CONFIG = {
    'population_size': 100,        # 小族群
    'generations': 10,             # 少代數
    'niching_enabled': True,
    'niching_n_clusters': 3,       # 少 niches
    'niching_update_frequency': 2, # 頻繁更新以觀察效果
}

這樣可以快速驗證功能是否正常運作。

================================================================================
""")

if __name__ == "__main__":
    print("\n請直接修改 run_portfolio_experiment.py 中的 CONFIG 並運行該腳本。")
    print("此文件僅為使用指南。\n")
